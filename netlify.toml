[build]
  command = "npm run build"
  publish = "dist"

[functions]
  directory = "netlify/functions"

[functions.node_bundle]
  external_node_modules = []

# Note: The schedule is defined in the function file itself (ESM syntax).
# No extra plugins are needed for scheduled functions in modern Netlify.
